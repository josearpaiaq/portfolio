import SnappingPage from "@/components/snappingPage";
import { sectionsConfig } from "@/constants";

export default function Projects() {
  return <SnappingPage id={sectionsConfig.projects.id}>Projects</SnappingPage>;
}
